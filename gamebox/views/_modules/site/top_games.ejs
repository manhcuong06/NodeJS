<div class="content">
    <div class="container">
        <div class="top-games">
            <h3>Top Games</h3>
            <span></span>
        </div>
        <div class="top-game-grids">
            <ul id="flexiselDemo1">
                <% data.products.forEach(product => { %>
                <li>
                    <div class="game-grid">
                        <h4><%= product.name %></h4>
                        <p><%= product.description %></p>
                        <a href="/site/detail/<%= product._id %>">
                            <img src="/images/<%= product.image %>" class="img-responsive" alt=""/>
                        </a>
                    </div>
                    <button class="btn btn-primary btn-block" onclick="updateCart(<%= product._id %>)">Buy (<%= product.price %>$)</button>
                </li>
                <% }); %>
            </ul>
        </div>
    </div>
</div>

<script>
$(window).load(function() {
    $("#flexiselDemo1").flexisel({
        visibleItems: 4,
        animationSpeed: 1000,
        autoPlay: true,
        autoPlaySpeed: 3000,
        pauseOnHover: true,
        enableResponsiveBreakpoints: true,
        responsiveBreakpoints: {
            portrait: {
                changePoint: 480,
                visibleItems: 1
            },
            landscape: {
                changePoint: 640,
                visibleItems: 2
            },
            tablet: {
                changePoint: 768,
                visibleItems: 3
            }
        }
    });
});
</script>
<script src="/js/jquery.flexisel.js"></script>